# Database
MONGO_URI=mongodb+srv://vibuproduction_db_user:4EbS8ojDnWnSgxki@cluster0.onhtmk6.mongodb.net/portfolio?appName=Cluster0

# JWT
# IMPORTANT: Change this to a random secure string in production!
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS
# For multiple origins, separate with commas (no spaces around commas)
# Note: All *.vercel.app URLs are automatically allowed, so you don't need to add each preview URL
# Include your production frontend URL and any custom domains
# Example: CLIENT_ORIGIN=http://localhost:5173,https://my-portfolio-eight-silk-68.vercel.app,https://www.yourdomain.com
# For single origin: CLIENT_ORIGIN=http://localhost:5173
# 
# IMPORTANT: All Vercel preview URLs (*.vercel.app) are automatically allowed by default
# You only need to add custom domains or specific production URLs here
CLIENT_ORIGIN=http://localhost:5173,https://my-portfolio-eight-silk-68.vercel.app

# Server
PORT=3001
NODE_ENV=development
